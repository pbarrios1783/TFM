streamlit==1.25.0
Pillow==9.2.0  # Para la manipulación de imágenes (PIL)
transformers==4.27.0  # Modelos de transformers como T5
torch==2.0.0  # Para PyTorch y el manejo de modelos
re  # Para operaciones con expresiones regulares
pandas==1.4.3  # Para manejo de dataframes y datos en Excel
matplotlib==3.5.2  # Para gráficos y visualizaciones
wordcloud==1.8.1  # Para manipulación de stopwords en wordclouds
safetensors==0.2.8  # Para cargar archivos de modelos en formato seguro
gdown==4.7.1  # Para descargar archivos desde Google Drive
zipfile36==0.1.3  # Para trabajar con archivos ZIP
os  # Biblioteca estándar para operaciones del sistema operativo
folium==0.12.1  # Para mapas interactivos
geopandas==0.10.2  # Para manipulación de datos geográficos (GeoJSON)
openai==0.27.0  # API de OpenAI para generación de texto
fpdf==1.7.2  # Para generar PDFs
base64  # Para manejo de archivos codificados en base64
dotenv==0.21.0  # Para manejar variables de entorno (como las API keys)
